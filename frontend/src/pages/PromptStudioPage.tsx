import { useState } from "react";
import { Sparkles, Copy, History, Eye } from "lucide-react";
import { PageHeader } from "@/components/page-header";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { ScrollArea } from "@/components/ui/scroll-area";
import { cn } from "@/lib/utils";
import { usePromptBundle, usePromptInventory, usePromptTemplates } from "@/hooks/usePromptStudio";
import { useDatabaseContext } from "@/context/database-context";

export function PromptStudioPage() {
  const [tab, setTab] = useState("system");
  const { selectedDatabaseId } = useDatabaseContext();
  const dbId = selectedDatabaseId ?? 1;
  const { data: templates } = usePromptTemplates();
  const { data: inventory } = usePromptInventory();
  const { data: bundle } = usePromptBundle(dbId);
  const versions = templates?.templates ?? [];
  const contexts: Record<string, string> = { system: bundle?.content?.slice(0, 1200) ?? "No prompt bundle available yet.", database: inventory?.prompts?.map((p) => `${p.prompt}: ${p.consumer}`).join("\n") ?? "No inventory available yet.", agent: bundle?.artifacts?.map((a) => `${a.artifact_type}: ${a.filename ?? "artifact"}`).join("\n") ?? "No agent context available yet.", rag: bundle?.bundle_filename ?? "No RAG context available yet.", sql: bundle?.bundle_mime ?? "No text-to-SQL context available yet." };
  return (<div className="space-y-6"><PageHeader eyebrow="AI Surface" title="Prompt Studio" description="Prompt artifacts and bundle previews generated from persisted intelligence packages." actions={<><Button variant="outline" size="sm" className="gap-1.5"><History className="h-3.5 w-3.5" /> Versions</Button><Button size="sm" className="gap-1.5 bg-gradient-to-br from-primary to-primary-glow text-primary-foreground"><Sparkles className="h-3.5 w-3.5" /> Publish</Button></>} /><div className="grid grid-cols-1 gap-4 xl:grid-cols-[1fr_320px]"><Card><CardHeader className="flex flex-row items-start justify-between gap-3 space-y-0"><div><CardTitle className="text-base">Prompt context</CardTitle><CardDescription>Composed from persisted intelligence packages.</CardDescription></div><div className="flex items-center gap-2"><Badge variant="outline" className="text-[11px]">db {dbId}</Badge><Badge variant="outline" className="text-[11px] tabular-nums">{(bundle?.content?.length ?? 0).toLocaleString()} chars</Badge></div></CardHeader><CardContent><Tabs value={tab} onValueChange={setTab}><TabsList className="flex-wrap"><TabsTrigger value="system">System</TabsTrigger><TabsTrigger value="database">Database</TabsTrigger><TabsTrigger value="agent">Agent</TabsTrigger><TabsTrigger value="rag">RAG</TabsTrigger><TabsTrigger value="sql">Text-to-SQL</TabsTrigger></TabsList>{Object.entries(contexts).map(([key, body]) => (<TabsContent key={key} value={key} className="pt-4"><div className="relative overflow-hidden rounded-md border border-border bg-[var(--muted)]/40"><div className="flex items-center justify-between border-b border-border bg-card/60 px-3 py-2 text-[11px] text-muted-foreground"><span className="font-mono">{key}.prompt.md</span><Button variant="ghost" size="sm" className="h-7 gap-1 px-2 text-[11px]"><Copy className="h-3 w-3" /> Copy</Button></div><ScrollArea className="max-h-[360px]"><pre className="whitespace-pre-wrap p-4 font-mono text-xs leading-relaxed text-foreground">{body}</pre></ScrollArea></div></TabsContent>))}</Tabs></CardContent></Card><div className="space-y-4"><Card><CardHeader><CardTitle className="text-base">Versions</CardTitle><CardDescription>Templates are sourced from the backend registry.</CardDescription></CardHeader><CardContent className="space-y-2">{versions.length ? versions.map((v, i) => (<div key={v.id} className={cn("flex items-start justify-between gap-3 rounded-md border p-3", i === 0 ? "border-primary/40 bg-primary/5" : "border-border bg-card")}><div className="min-w-0"><div className="flex items-center gap-2 text-sm font-medium text-foreground">{v.name}{i === 0 && <Badge variant="outline" className="border-primary/40 bg-primary/10 text-primary text-[10px]">live</Badge>}</div><div className="text-[11px] text-muted-foreground">{v.category} · {v.version}</div></div><Badge variant="outline" className="shrink-0 tabular-nums text-[10px]">{v.language}</Badge></div>)) : <div className="text-sm text-muted-foreground">No versions available yet.</div>}</CardContent></Card><Card><CardHeader><CardTitle className="text-base">Preview</CardTitle><CardDescription>Dry-run the assembled prompt against the active source.</CardDescription></CardHeader><CardContent><Button variant="outline" size="sm" className="w-full gap-1.5"><Eye className="h-3.5 w-3.5" /> Open preview</Button></CardContent></Card></div></div></div>);
}
