import { Boxes, FileText, Cpu, Search } from "lucide-react";
import { PageHeader } from "@/components/page-header";
import { MetricCard } from "@/components/metric-card";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { StatusBadge, type StatusKind } from "@/components/status-badge";
import { CoverageBar } from "@/components/coverage-bar";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { useEmbeddings } from "@/hooks/useEmbeddings";
import { useDatabaseContext } from "@/context/database-context";

export function EmbeddingsPage() {
  const { selectedDatabaseId } = useDatabaseContext();
  const dbId = selectedDatabaseId ?? 1;
  const { data } = useEmbeddings(dbId);
  const collections = data?.collections ?? [];
  return (<div className="space-y-6"><PageHeader eyebrow="AI Surface" title="Embeddings & retrieval" description="Vector collections and indexing status generated from persisted intelligence packages." actions={<Badge variant="outline" className="gap-1.5 text-[11px]"><span className="h-1.5 w-1.5 rounded-full bg-[var(--success)] shadow-[0_0_6px_var(--success)]" /> {data?.qdrant_health ? "Qdrant · healthy" : "Qdrant · unavailable"}</Badge>} /><section className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4"><MetricCard label="Collections" value={String(collections.length)} icon={Boxes} tone="info" /><MetricCard label="Documents indexed" value={String(data?.indexed_tables ?? 0)} icon={FileText} tone="default" /><MetricCard label="Vectors" value={String(data?.vectors_total ?? 0)} hint={`model: ${data?.embedding_model ?? "n/a"}`} icon={Cpu} tone="success" /><MetricCard label="Embedding coverage" value={`${Math.round(((data?.completed_tables ?? 0) / Math.max(1, data?.indexed_tables ?? 1)) * 100)}%`} icon={Boxes} progress={Math.round(((data?.completed_tables ?? 0) / Math.max(1, data?.indexed_tables ?? 1)) * 100)} tone="success" /></section><Card><CardHeader><CardTitle className="text-base">Semantic search</CardTitle><CardDescription>Query any indexed collection. Returns ranked results with similarity scores.</CardDescription></CardHeader><CardContent className="space-y-3"><div className="grid grid-cols-[minmax(0,1fr)_auto] gap-2 sm:grid-cols-[160px_minmax(0,1fr)_auto]"><Input defaultValue={data?.collections?.[0]?.collection_name ?? "n/a"} className="h-9 font-mono text-xs sm:col-span-1" /><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 h-3.5 w-3.5 -translate-y-1/2 text-muted-foreground" /><Input placeholder="Search the indexed packages..." className="h-9 pl-9" /></div><Button size="sm" className="bg-gradient-to-br from-primary to-primary-glow text-primary-foreground">Search</Button></div></CardContent></Card><Card><CardHeader><CardTitle className="text-base">Embedding collections</CardTitle><CardDescription>All vector collections registered for retrieval.</CardDescription></CardHeader><CardContent><div className="overflow-x-auto"><Table><TableHeader><TableRow className="bg-muted/40 hover:bg-muted/40"><TableHead>Collection</TableHead><TableHead className="text-right">Vectors</TableHead><TableHead className="text-right">Indexed tables</TableHead><TableHead className="min-w-[160px]">Coverage</TableHead><TableHead>Status</TableHead></TableRow></TableHeader><TableBody>{collections.length ? collections.map((c) => (<TableRow key={c.collection_name}><TableCell className="font-mono text-sm">{c.collection_name}</TableCell><TableCell className="text-right tabular-nums">{c.vectors.toLocaleString()}</TableCell><TableCell className="text-right tabular-nums">{c.indexed_tables ?? 0}</TableCell><TableCell><CoverageBar value={Math.min(100, c.vectors ? 100 : 0)} /></TableCell><TableCell><StatusBadge status={(data?.embedding_health ? "success" : "warning") as StatusKind} /></TableCell></TableRow>)) : <TableRow><TableCell colSpan={5} className="text-sm text-muted-foreground">No embedding collections available yet.</TableCell></TableRow>}</TableBody></Table></div></CardContent></Card></div>);
}
