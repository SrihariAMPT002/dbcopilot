import { Link } from "@tanstack/react-router";
import { Database, Plus, RefreshCw, MoreHorizontal, Search } from "lucide-react";
import { PageHeader } from "@/components/page-header";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Badge } from "@/components/ui/badge";
import { StatusBadge, type StatusKind } from "@/components/status-badge";
import { CoverageBar } from "@/components/coverage-bar";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "@/components/ui/dropdown-menu";
import { useConnections } from "@/hooks/useConnections";

export function SourcesPage() {
  const { data = [] } = useConnections();
  const sources = data.map((s) => ({ name: s.name, engine: s.db_type, env: "production", host: s.host, schemas: s.schema_count ?? 0, tables: s.table_count ?? 0, coverage: Math.min(100, s.table_count ? 80 : 0), status: (s.status === "active" ? "success" : s.status) as StatusKind, lastSync: s.last_sync_at ? new Date(s.last_sync_at).toLocaleString() : "unknown" }));
  return (<div className="space-y-6"><PageHeader eyebrow="Sources" title="Connected sources" description="All registered database connections backed by the live backend." actions={<><Button variant="outline" size="sm" className="gap-1.5"><RefreshCw className="h-3.5 w-3.5" /> Sync all</Button><Button asChild size="sm" className="gap-1.5 bg-gradient-to-br from-primary to-primary-glow text-primary-foreground"><Link to="/connect"><Plus className="h-3.5 w-3.5" /> Add source</Link></Button></>} /><Card><CardContent className="p-4"><div className="grid grid-cols-[minmax(0,1fr)_auto] items-center gap-3 sm:flex sm:flex-wrap sm:justify-between"><div className="relative min-w-0 flex-1 sm:max-w-sm"><Search className="pointer-events-none absolute left-3 top-1/2 h-3.5 w-3.5 -translate-y-1/2 text-muted-foreground" /><Input placeholder="Search sources…" className="h-9 pl-9" /></div><div className="flex shrink-0 items-center gap-2"><Badge variant="outline" className="text-[11px]">All engines</Badge><Badge variant="outline" className="text-[11px]">All environments</Badge></div></div><div className="mt-4 overflow-x-auto"><Table><TableHeader><TableRow className="bg-muted/40 hover:bg-muted/40"><TableHead className="min-w-[200px]">Source</TableHead><TableHead>Engine</TableHead><TableHead>Env</TableHead><TableHead className="text-right">Schemas</TableHead><TableHead className="text-right">Tables</TableHead><TableHead className="min-w-[160px]">AI coverage</TableHead><TableHead>Sync</TableHead><TableHead>Last sync</TableHead><TableHead className="w-10"></TableHead></TableRow></TableHeader><TableBody>{sources.map((s) => (<TableRow key={s.name} className="hover:bg-muted/30"><TableCell className="font-medium"><div className="flex min-w-0 items-center gap-2"><div className="grid h-7 w-7 shrink-0 place-items-center rounded-md bg-gradient-to-br from-primary/15 to-primary/0 text-primary"><Database className="h-3.5 w-3.5" /></div><div className="min-w-0"><div className="truncate text-sm text-foreground">{s.name}</div><div className="truncate text-[11px] text-muted-foreground">{s.host}</div></div></div></TableCell><TableCell className="text-sm text-muted-foreground">{s.engine}</TableCell><TableCell><Badge variant="outline" className="text-[10px] uppercase tracking-wider">{s.env}</Badge></TableCell><TableCell className="text-right tabular-nums">{s.schemas}</TableCell><TableCell className="text-right tabular-nums">{s.tables}</TableCell><TableCell className="min-w-[160px]"><CoverageBar value={s.coverage} /></TableCell><TableCell><StatusBadge status={s.status} /></TableCell><TableCell className="text-xs text-muted-foreground">{s.lastSync}</TableCell><TableCell className="text-right"><DropdownMenu><DropdownMenuTrigger asChild><Button variant="ghost" size="icon" className="h-7 w-7"><MoreHorizontal className="h-4 w-4" /></Button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem>Open explorer</DropdownMenuItem><DropdownMenuItem>Run sync</DropdownMenuItem><DropdownMenuItem>Regenerate AI packages</DropdownMenuItem><DropdownMenuItem className="text-destructive">Disconnect</DropdownMenuItem></DropdownMenuContent></DropdownMenu></TableCell></TableRow>))}</TableBody></Table></div></CardContent></Card></div>);
}
