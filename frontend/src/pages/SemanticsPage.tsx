import { BookOpenText, Building2, Workflow, Sparkles, Search } from "lucide-react";
import { PageHeader } from "@/components/page-header";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Input } from "@/components/ui/input";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion";
import { useSemantics } from "@/hooks/useSemantics";

export function SemanticsPage() {
  const dbId = 1;
  const { data } = useSemantics(dbId);
  const entities = data?.business_entities ?? [];
  const processes = data?.business_processes ?? [];
  const capabilities = data?.business_capabilities ?? [];
  const glossary = data?.business_glossary ?? [];
  return (<div className="space-y-6"><PageHeader eyebrow="Intelligence" title="Semantics" description="Business domain, entities, processes, capabilities, and shared glossary from persisted semantic packages." /><section className="grid grid-cols-1 gap-4 xl:grid-cols-3"><Card className="xl:col-span-2 overflow-hidden"><div className="relative"><div className="pointer-events-none absolute inset-0 bg-gradient-to-br from-primary/10 via-transparent to-transparent" /><CardHeader className="relative"><div className="flex items-center gap-2"><Badge variant="outline" className="border-primary/30 bg-primary/10 text-primary"><Building2 className="mr-1 h-3 w-3" /> Domain</Badge><Badge variant="outline" className="text-[11px]">{data?.business_domain ?? "Pending discovery"}</Badge></div><CardTitle className="text-base">Business summary</CardTitle><CardDescription>{data?.semantic_summary ?? "No semantic summary available yet for this database."}</CardDescription></CardHeader></div><CardContent className="space-y-4 pt-0"><Tabs defaultValue="entities"><TabsList><TabsTrigger value="entities">Entities</TabsTrigger><TabsTrigger value="processes">Processes</TabsTrigger><TabsTrigger value="capabilities">Capabilities</TabsTrigger></TabsList><TabsContent value="entities" className="pt-4"><div className="flex flex-wrap gap-2">{entities.length ? entities.map((e) => (<Badge key={e} variant="outline" className="gap-1 rounded-full border-border bg-card px-2.5 py-1 text-xs"><Sparkles className="h-3 w-3 text-primary" /> {e}</Badge>)) : <div className="text-sm text-muted-foreground">No semantic entities found yet.</div>}</div></TabsContent><TabsContent value="processes" className="space-y-3 pt-4">{processes.length ? processes.map((p) => (<div key={p} className="rounded-md border border-border bg-card p-3"><div className="mb-2 flex items-center gap-2 text-sm font-medium text-foreground"><Workflow className="h-4 w-4 text-primary" /> {p}</div></div>)) : <div className="text-sm text-muted-foreground">No semantic processes found yet.</div>}</TabsContent><TabsContent value="capabilities" className="grid grid-cols-2 gap-2 pt-4 sm:grid-cols-3">{capabilities.length ? capabilities.map((c) => (<div key={c} className="rounded-md border border-border bg-card px-3 py-2 text-sm text-foreground">{c}</div>)) : <div className="text-sm text-muted-foreground">No capabilities found yet.</div>}</TabsContent></Tabs></CardContent></Card><Card><CardHeader><CardTitle className="text-base">Glossary</CardTitle><CardDescription>Shared definitions used across packages.</CardDescription><div className="relative pt-2"><Search className="pointer-events-none absolute left-3 top-1/2 h-3.5 w-3.5 -translate-y-1/2 text-muted-foreground" /><Input placeholder="Search terms…" className="h-8 pl-9 text-sm" /></div></CardHeader><CardContent><Accordion type="single" collapsible className="w-full">{glossary.length ? glossary.map((g) => (<AccordionItem key={g.term} value={g.term}><AccordionTrigger className="text-sm hover:no-underline"><span className="flex items-center gap-2"><BookOpenText className="h-3.5 w-3.5 text-primary" />{g.term}</span></AccordionTrigger><AccordionContent className="text-xs text-muted-foreground">{g.definition}</AccordionContent></AccordionItem>)) : <div className="text-sm text-muted-foreground">No glossary terms found yet.</div>}</Accordion></CardContent></Card></section></div>);
}
